# LUMOS gateway — Phase 1 dependencies.
#
# Pin only the things the wire depends on; everything else floats so we can
# pull SDK fixes without a rebuild.

# Web framework + ASGI
fastapi>=0.110,<0.150
uvicorn[standard]>=0.28
websockets>=12.0

# Data + config
pydantic>=2.6
python-dotenv>=1.0

# LLM brain
google-genai>=1.0

# TTS (optional at install time; MockTTS kicks in if missing)
cartesia>=1.0; python_version >= "3.10"

# Display renderer
matplotlib>=3.7
numpy>=1.24
Pillow>=10.0

# Test deps
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
